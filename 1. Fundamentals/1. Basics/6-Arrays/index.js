let selectedColor = ['red', 'blue'];    // Define an array

selectedColor[2] = 1;

// The size of array is dynamic.
console.log(selectedColor);

// Array is an object in Javascript, so you can use dot notation on an array
console.log(typeof selectedColor);
console.log(selectedColor.length);      // Output: 3